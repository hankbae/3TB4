module reaction_game(
	input        CLOCK_50,
	input	 [3:0] KEY,
	output [6:0] HEX0,
					 HEX1,
					 HEX2,
					 HEX3,
					 HEX4,
					 HEX5,
	output [9:0] LEDR,
	);
	
	
	
	wire clk;
	wire [19:0] divisor,startTime;
	assign divisor = 20'b4;		// divisor for clock
	
	clock_divider msec(CLOCK_50,KEY[1],divisor,clk);
	hex_counter startDelay(clk,KEY[1],1'b0,1'b1,startTime);
	
	always @(posedge CLOCK_50)
	begin
		if (startTime >= 20'd2000)
	
	end
	
	
	
	
endmodule
